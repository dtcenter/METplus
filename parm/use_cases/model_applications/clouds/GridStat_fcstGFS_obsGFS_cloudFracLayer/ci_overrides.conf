[config]
# CI OVERRIDE for model_applications/clouds/GridStat_fcstGFS_obsGFS_cloudFracLayer.conf
# these settings can overrides the settings in the use case conf so they run in a reasonable amount
# of time in automation

# Specifically, we are only running one lead time for MODE rather than 4 lead times in the automated
# tests.

[low_cloud]
LEAD_SEQ = 6


[mid_cloud]
LEAD_SEQ = 6


[high_cloud]
LEAD_SEQ = 6
