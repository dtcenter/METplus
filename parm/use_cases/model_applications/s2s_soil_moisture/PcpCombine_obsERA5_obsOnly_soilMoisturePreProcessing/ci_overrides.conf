[config]
# CI OVERRIDE for model_applications/s2s_soil_moisture/PcpCombine_obsERA5_obsOnly_soilMoisturePreProcessing.conf
# Using the full settings requires 35m to run, so these settings can overrides the settings in the use case 
# conf so they run in a reasonable amount of time in automation.

VALID_BEG = 2018010100

[obs_mean_stdev]

OBS_PCP_COMBINE_DERIVE_LOOKBACK = 3Y
