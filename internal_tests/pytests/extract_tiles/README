Run the test as follows:

1) cd to METplus/internal_tests/pytests/extract_tiles

2) run the following from the command line:

py.test -c <path/to>/extract_tiles_test.conf -c <path/to>/custom.conf

!!!!!!!!!!!!!!!
!!!IMPORTANT!!!
!!!!!!!!!!!!!!!
Before you run the test, make sure to update the PROJ_DIR (in the custom.conf file) to your location of the METplus source code
